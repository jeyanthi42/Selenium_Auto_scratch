package CommanUse;

import java.io.File;

import org.apache.commons.io.FileUtils;
import org.openqa.selenium.OutputType;
import org.openqa.selenium.TakesScreenshot;
import org.openqa.selenium.WebDriver;

import Hooks.BasicHooksClass;
import io.github.bonigarcia.wdm.WebDriverManager;

public class CommonUtil {
	BasicHooksClass hooks ; 
			WebDriver driver;
	public void takingScreenshot() {
		
//		try {
//		driver= lauchingBrowse.getInstance(0).getDriverName();
//		TakesScreenshot screenshot = (TakesScreenshot)driver;
//		File file = screenshot.getScreenshotAs(OutputType.FILE);
//		String destFilename = hooks.getScenarioName()+".png";
//		File file2 = new File(destFilename);
//		FileUtils.copyFile(file, file2);
//		} catch (Exception e) {
//			e.printStackTrace();
//			// TODO: handle exception
//		}
	//	FileUtils.copyFile(file, new File (destFilena+".png"));
		
		
		
		}
}
